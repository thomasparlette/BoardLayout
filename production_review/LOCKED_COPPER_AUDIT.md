# Locked copper audit summary

The native board contains **3339 locked segments** and **89 locked vias**. `LOCKED_COPPER_AUDIT.csv` groups them by net and layer/span. This is an inventory only: route purpose and justification still require engineering review before any lock is removed.
